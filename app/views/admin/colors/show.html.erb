<p style="color: green"><%= notice %></p>

<%= render @color %>

<div>
  <%= link_to "Edit this color", edit_admin_color_path(@color) %> |
  <%= link_to "Back to colors", admin_colors_path %>
  <%= link_to "Destroy this color", [:admin, @color], data: { turbo_stream: false, turbo_method: :delete, turbo_confirm: "Are you sure?" } %>
</div>
